# Terraform Placeholder

This project is currently optimized for local Docker Compose demos. A production deployment can map the same services to:

- API and worker on Cloud Run, Fly.io, Render, or Kubernetes
- Postgres with pgvector enabled
- Redis managed cache
- Prometheus/Grafana or hosted OpenTelemetry-compatible observability
